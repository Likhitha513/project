// Toggle mobile navigation menu
function toggleMenu() {
  const menu = document.getElementById("navMenu");
  menu.classList.toggle("active");
}

// Simulate latest alert pop-up
function showAlert() {
  alert("✅ No active disaster alerts at the moment. Stay safe!");
}
